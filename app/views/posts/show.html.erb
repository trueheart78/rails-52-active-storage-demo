<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<% if @post.image.attached? %>
<!-- @post.image.present? will always return true, use attached? to check presence -->
  <p>
    <strong>Image:</strong>
    <br>
    <%= image_tag @post.image %>
    <br>
    <%= url_for @post.image %>
  </p>
<% end %> 

<% if @post.second_image.attached? %>
<!-- @post.second_image.present? will always return true, use attached? to check presence -->
  <p>
    <strong>Second Image:</strong>
    <br>
    <%= image_tag @post.second_image %>
  </p>
<% end %> 

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
